#
# see also src/utiladm/kctl

krc ()
{
	(($#)) && { export KUBECONFIG=~/.kube/$1.yml; return; }
	printf "$KUBECONFIG${KUBECONFIG:+\n}"
}

kns ()
{
	(($#)) && { export KUBENS=${1:?}; return; }
	printf "$KUBENS${KUBENS:+\n}"
}
