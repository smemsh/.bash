#

# this is just a convenience rather than typing it all the
# time.  I used to have an alias `s' that did this same thing but then it
# wouldn't work if there were shell redirects and such.  Now I just surround
# it in quotes and give it to
r ()
{
	sudo sh -c "${@}"
}
