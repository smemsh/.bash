#

# this is just a convenience rather than typing it all the
# time.  used to have an alias `s' that did this same
# thing but then it wouldn't work if there were shell
# redirects and such.  now we just surround it in quotes and
# give it to a nested shell to interpret
#
r ()
{
	sudo sh -c "${@}"
}
