#
# run control settings for the `go*' suite of programs
#

GOBASE=~/var/go				# everything subordinate to this loc
GOASSOC=${GOASSOC:-none}		# make sure set to something

GOBASE_ASSOC=$GOBASE/$HOSTNAME/assoc	# where we keep all our association dirs
GOBASE_TYPE_PTY=$GOBASE/pts		# where we keep all our links by pty
GOBASE_TYPE_TTY=$GOBASE/tty		# and by tty

GOLOGNAME=cmdlog			# logfilename, bash format
GOLOGBASH=bashlog			# logfilename, go format

GOTTY_TYPE=${TERMINAL/\//}		# for `pts/X' strip off the `/'
GOTTY_TYPE=${TERMINAL:0:3}		# `pts' or `tty'
GOTTY_NUM=${TERMINAL:3}			# the raw number of the tty
GOTTY_NUM=${GOTTY_NUM#/}		# strip leading `/' if exists (for pts)

GOASSOCDIR=$GOBASE_ASSOC/$GOASSOC	# where all the stuff goes for an assoc

GOLOG_CMDS=$GOASSOCDIR/$GOLOGNAME	# logfile for our format logs
GOLOG_BASH=$GOASSOCDIR/$GOLOGBASH	# logfile for bash format logs
GOLOG_FULL=~/var/log/history.$HOSTNAME	# bash format log of all shells
GOLOG_MOST=~/var/log/cmdhist.$HOSTNAME	# our format log of all shells

# allow lookups by tty
GOINSTANCE=$GOBASE/$HOSTNAME/$GOTTY_TYPE/$GOTTY_NUM/$SHLVL
